import { ListViewHostDirective } from './list-view-host.directive';

describe('ListViewHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ListViewHostDirective();
    expect(directive).toBeTruthy();
  });
});
